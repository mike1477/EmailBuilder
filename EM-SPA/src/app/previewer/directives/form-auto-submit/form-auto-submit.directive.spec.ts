import { FormAutoSubmitDirective } from './form-auto-submit.directive';

describe('FormAutoSubmitDirective', () => {
  it('should create an instance', () => {
    const directive = new FormAutoSubmitDirective();
    expect(directive).toBeTruthy();
  });
});
